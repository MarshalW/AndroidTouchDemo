<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:layout_width="fill_parent"
             android:layout_height="fill_parent"
        >
    <WebView
            android:id="@+id/webView"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            />
    <com.example.touch.CustomerViewGroup
            android:id="@+id/topView"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:alpha="0.7"
            android:background="#ffecd2">
        <TextView
                android:layout_width="700dp"
                android:layout_height="fill_parent"
                android:text="A very large text than must be shown in the textview, but no more of three lines are going to be shown A very large text than must be shown in "
                android:maxLines="30"
                android:lines="3"
                android:textSize="60dp"
                />
    </com.example.touch.CustomerViewGroup>
</FrameLayout>

